import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  topHeader:{
    backgroundColor: '#282c34',
    color:'white',
    textAlign:'center',
    padding: '24px ',
    marginBottom:'10px'
  }
}))