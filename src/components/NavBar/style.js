import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  textFieldWrapper: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-evenly",
  },

  checkboxWrapper: {
    display: "flex",
    flexDirection: "row",
  },
}));
